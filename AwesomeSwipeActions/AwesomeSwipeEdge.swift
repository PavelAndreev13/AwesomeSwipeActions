import SwiftUI

/// The horizontal edge from which swipe actions are revealed.
public enum SwipeActionEdge: Sendable {
    /// Actions are revealed by swiping from the leading (left) edge.
    case leading
    /// Actions are revealed by swiping from the trailing (right) edge.
    case trailing
}

